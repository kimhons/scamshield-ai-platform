import React, { useState, useEffect } from 'react'
import { useNavigate } from 'react-router-dom'
import { 
  ArrowRight, Play, CheckCircle, Zap, Shield, Brain, 
  Eye, Search, BarChart3, Globe, Users, Award,
  Star, ChevronRight, Sparkles
} from 'lucide-react'

const NexusLandingPage = () => {
  const navigate = useNavigate()
  const [activeFeature, setActiveFeature] = useState(0)

  // Auto-rotate features every 3 seconds
  useEffect(() => {
    const interval = setInterval(() => {
      setActiveFeature((prev) => (prev + 1) % 3)
    }, 3000)
    return () => clearInterval(interval)
  }, [])

  const stats = [
    { value: '99.9%', label: 'Detection Accuracy', sublabel: 'Industry leading' },
    { value: '<1s', label: 'Response Time', sublabel: 'Lightning fast' },
    { value: '500+', label: 'Organizations', sublabel: 'Trust Nexus Guard' },
    { value: '$10M+', label: 'Threats Prevented', sublabel: 'Value protected' }
  ]

  const features = [
    {
      icon: Brain,
      title: 'AI-Powered Intelligence',
      description: 'Advanced machine learning algorithms that adapt and learn from new threat patterns in real-time.',
      benefits: ['Real-time threat analysis', 'Behavioral pattern recognition', 'Predictive threat modeling', 'Zero-day detection']
    },
    {
      icon: Eye,
      title: 'Continuous Monitoring',
      description: 'Round-the-clock surveillance with intelligent alerting that focuses on what matters most.',
      benefits: ['24/7 automated monitoring', 'Smart alert prioritization', 'Multi-vector analysis', 'Risk score assessment']
    },
    {
      icon: Shield,
      title: 'Enterprise Security',
      description: 'Bank-grade security infrastructure with compliance standards built for enterprise scale.',
      benefits: ['SOC 2 Type II certified', 'GDPR & CCPA compliant', 'End-to-end encryption', 'Audit trail logging']
    }
  ]

  const trustedBy = [
    { name: 'Microsoft', logo: '🏢' },
    { name: 'Tesla', logo: '⚡' },
    { name: 'Stripe', logo: '💳' },
    { name: 'Notion', logo: '📝' },
    { name: 'Linear', logo: '📐' },
    { name: 'Vercel', logo: '▲' }
  ]

  const pricingTiers = [
    {
      name: 'Starter',
      price: '$49',
      period: 'per user/month',
      description: 'Perfect for growing teams',
      features: [
        'Up to 100 investigations/month',
        'Basic threat intelligence',
        'Email support',
        'Standard dashboards'
      ],
      cta: 'Start Free Trial',
      featured: false
    },
    {
      name: 'Professional',
      price: '$149',
      period: 'per user/month',
      description: 'Advanced features for professionals',
      features: [
        'Unlimited investigations',
        'Advanced threat intelligence',
        'Priority support',
        'Custom dashboards',
        'API access',
        'Advanced analytics'
      ],
      cta: 'Start Free Trial',
      featured: true
    },
    {
      name: 'Enterprise',
      price: 'Custom',
      period: 'contact sales',
      description: 'Tailored for large organizations',
      features: [
        'Everything in Professional',
        'Dedicated success manager',
        'Custom integrations',
        'On-premise deployment',
        'SLA guarantees',
        'Advanced compliance'
      ],
      cta: 'Contact Sales',
      featured: false
    }
  ]

  return (
    <div className="bg-white">
      {/* Hero Section */}
      <section className="relative min-h-screen flex items-center justify-center overflow-hidden">
        {/* Background Elements */}
        <div className="absolute inset-0 bg-gradient-to-br from-blue-50 via-white to-purple-50"></div>
        <div className="absolute top-20 left-10 w-72 h-72 bg-blue-500/10 rounded-full blur-3xl"></div>
        <div className="absolute bottom-20 right-10 w-96 h-96 bg-purple-500/10 rounded-full blur-3xl"></div>
        
        <div className="nexus-container relative z-10">
          <div className="text-center max-w-4xl mx-auto">
            {/* Badge */}
            <div className="inline-flex items-center space-x-2 bg-white/80 backdrop-blur-sm border border-gray-200 rounded-full px-4 py-2 mb-8">
              <Sparkles className="w-4 h-4 text-blue-500" />
              <span className="text-sm font-medium text-gray-700">Introducing Nexus Guard AI Platform</span>
              <ChevronRight className="w-4 h-4 text-gray-400" />
            </div>

            {/* Main Headline */}
            <h1 className="nexus-heading-1 mb-6 nexus-animate-fade-in">
              AI-Powered Intelligence
              <br />
              <span className="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                for Modern Security
              </span>
            </h1>

            {/* Subheadline */}
            <p className="nexus-body-large text-gray-600 mb-12 max-w-2xl mx-auto nexus-animate-slide-up">
              Detect, investigate, and respond to threats with unprecedented speed and accuracy. 
              Built for teams who demand excellence in security operations.
            </p>

            {/* CTA Buttons */}
            <div className="flex flex-col sm:flex-row gap-4 justify-center mb-16 nexus-animate-slide-up">
              <button
                onClick={() => navigate('/signup')}
                className="nexus-btn nexus-btn-primary text-base px-8 py-4"
              >
                Start Free Trial
                <ArrowRight className="w-5 h-5 ml-2" />
              </button>
              <button className="inline-flex items-center space-x-2 text-gray-600 hover:text-gray-900 font-medium transition-colors">
                <div className="w-10 h-10 bg-white/80 backdrop-blur-sm border border-gray-200 rounded-full flex items-center justify-center">
                  <Play className="w-4 h-4 text-gray-600" />
                </div>
                <span>Watch Demo</span>
              </button>
            </div>

            {/* Stats */}
            <div className="grid grid-cols-2 md:grid-cols-4 gap-8 nexus-animate-slide-up">
              {stats.map((stat, index) => (
                <div key={index} className="text-center">
                  <div className="text-2xl md:text-3xl font-bold text-gray-900 mb-1">{stat.value}</div>
                  <div className="text-sm font-medium text-gray-700 mb-1">{stat.label}</div>
                  <div className="text-xs text-gray-500">{stat.sublabel}</div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </section>

      {/* Trusted By Section */}
      <section className="py-16 border-b border-gray-100">
        <div className="nexus-container">
          <p className="text-center text-gray-500 text-sm mb-8">Trusted by leading organizations worldwide</p>
          <div className="grid grid-cols-3 md:grid-cols-6 gap-8 items-center">
            {trustedBy.map((company, index) => (
              <div key={index} className="text-center">
                <div className="text-3xl mb-2">{company.logo}</div>
                <div className="text-sm font-medium text-gray-600">{company.name}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section id="features" className="nexus-section">
        <div className="nexus-container">
          <div className="text-center mb-16">
            <h2 className="nexus-heading-2 mb-4">
              Built for Modern Security Teams
            </h2>
            <p className="nexus-body-large text-gray-600 max-w-2xl mx-auto">
              Every feature designed with security professionals in mind. 
              No complexity, just powerful tools that work.
            </p>
          </div>

          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
            {features.map((feature, index) => {
              const Icon = feature.icon
              const isActive = activeFeature === index
              return (
                <div
                  key={index}
                  className={`nexus-card p-8 cursor-pointer transition-all duration-500 ${
                    isActive ? 'ring-2 ring-blue-500 shadow-xl' : ''
                  }`}
                  onMouseEnter={() => setActiveFeature(index)}
                >
                  <div className={`w-12 h-12 rounded-xl flex items-center justify-center mb-6 transition-all duration-300 ${
                    isActive 
                      ? 'bg-gradient-to-br from-blue-500 to-purple-600' 
                      : 'bg-gray-100'
                  }`}>
                    <Icon className={`w-6 h-6 ${isActive ? 'text-white' : 'text-gray-600'}`} />
                  </div>
                  
                  <h3 className="nexus-heading-3 mb-4">{feature.title}</h3>
                  <p className="nexus-body text-gray-600 mb-6">{feature.description}</p>
                  
                  <ul className="space-y-2">
                    {feature.benefits.map((benefit, idx) => (
                      <li key={idx} className="flex items-center text-sm text-gray-600">
                        <CheckCircle className="w-4 h-4 text-green-500 mr-2 flex-shrink-0" />
                        {benefit}
                      </li>
                    ))}
                  </ul>
                </div>
              )
            })}
          </div>
        </div>
      </section>

      {/* Security Section */}
      <section id="security" className="nexus-section bg-gray-50">
        <div className="nexus-container">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
            <div>
              <h2 className="nexus-heading-2 mb-6">
                Enterprise-Grade Security
              </h2>
              <p className="nexus-body-large text-gray-600 mb-8">
                Built with security and compliance at its core. 
                Your data is protected with the same standards used by financial institutions.
              </p>
              
              <div className="grid grid-cols-2 gap-6 mb-8">
                <div className="nexus-card p-6 text-center">
                  <Award className="w-8 h-8 text-blue-500 mx-auto mb-3" />
                  <div className="font-semibold text-gray-900 mb-1">SOC 2 Type II</div>
                  <div className="text-sm text-gray-600">Certified</div>
                </div>
                <div className="nexus-card p-6 text-center">
                  <Shield className="w-8 h-8 text-green-500 mx-auto mb-3" />
                  <div className="font-semibold text-gray-900 mb-1">GDPR</div>
                  <div className="text-sm text-gray-600">Compliant</div>
                </div>
              </div>

              <div className="flex items-center space-x-4">
                <div className="w-2 h-2 bg-green-500 rounded-full"></div>
                <span className="text-sm text-gray-600">99.9% uptime SLA</span>
                <div className="w-2 h-2 bg-green-500 rounded-full"></div>
                <span className="text-sm text-gray-600">End-to-end encryption</span>
              </div>
            </div>
            
            <div className="relative">
              <div className="nexus-card p-8 bg-gradient-to-br from-blue-50 to-purple-50">
                <div className="flex items-center justify-between mb-6">
                  <h3 className="font-semibold text-gray-900">Security Dashboard</h3>
                  <div className="flex items-center space-x-2">
                    <div className="w-2 h-2 bg-green-500 rounded-full"></div>
                    <span className="text-sm text-gray-600">Secure</span>
                  </div>
                </div>
                
                <div className="space-y-4">
                  <div className="flex items-center justify-between">
                    <span className="text-sm text-gray-600">Threat Level</span>
                    <span className="text-sm font-medium text-green-600">Low</span>
                  </div>
                  <div className="flex items-center justify-between">
                    <span className="text-sm text-gray-600">Active Monitors</span>
                    <span className="text-sm font-medium text-gray-900">247</span>
                  </div>
                  <div className="flex items-center justify-between">
                    <span className="text-sm text-gray-600">Response Time</span>
                    <span className="text-sm font-medium text-blue-600">&lt;1s</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Pricing Section */}
      <section id="pricing" className="nexus-section">
        <div className="nexus-container">
          <div className="text-center mb-16">
            <h2 className="nexus-heading-2 mb-4">
              Simple, Transparent Pricing
            </h2>
            <p className="nexus-body-large text-gray-600 max-w-2xl mx-auto">
              Choose the plan that fits your team size and security needs. 
              All plans include our core security features.
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            {pricingTiers.map((tier, index) => (
              <div
                key={index}
                className={`nexus-card p-8 relative ${
                  tier.featured ? 'ring-2 ring-blue-500 scale-105' : ''
                }`}
              >
                {tier.featured && (
                  <div className="absolute -top-4 left-1/2 transform -translate-x-1/2">
                    <span className="bg-gradient-to-r from-blue-500 to-purple-600 text-white px-4 py-1 rounded-full text-sm font-medium">
                      Most Popular
                    </span>
                  </div>
                )}
                
                <div className="text-center mb-8">
                  <h3 className="nexus-heading-3 mb-2">{tier.name}</h3>
                  <p className="text-gray-600 mb-4">{tier.description}</p>
                  <div className="text-4xl font-bold text-gray-900 mb-1">
                    {tier.price}
                    {tier.price !== 'Custom' && <span className="text-lg font-normal text-gray-600">/{tier.period}</span>}
                  </div>
                  {tier.price === 'Custom' && (
                    <p className="text-gray-600">{tier.period}</p>
                  )}
                </div>
                
                <ul className="space-y-3 mb-8">
                  {tier.features.map((feature, idx) => (
                    <li key={idx} className="flex items-center text-sm text-gray-600">
                      <CheckCircle className="w-4 h-4 text-green-500 mr-3 flex-shrink-0" />
                      {feature}
                    </li>
                  ))}
                </ul>
                
                <button
                  onClick={() => navigate(tier.cta === 'Contact Sales' ? '/contact' : '/signup')}
                  className={`w-full py-3 px-4 rounded-lg font-medium transition-all duration-200 ${
                    tier.featured
                      ? 'nexus-btn-primary'
                      : 'bg-gray-100 text-gray-900 hover:bg-gray-200'
                  }`}
                >
                  {tier.cta}
                </button>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="nexus-section bg-gradient-to-br from-blue-600 to-purple-700 text-white">
        <div className="nexus-container text-center">
          <h2 className="text-3xl md:text-4xl font-bold mb-4">
            Ready to Transform Your Security Operations?
          </h2>
          <p className="text-xl text-blue-100 mb-8 max-w-2xl mx-auto">
            Join hundreds of security teams who trust Nexus Guard to protect their organizations. 
            Start your free trial today.
          </p>
          
          <div className="flex flex-col sm:flex-row gap-4 justify-center">
            <button
              onClick={() => navigate('/signup')}
              className="bg-white text-blue-600 hover:bg-gray-100 px-8 py-4 rounded-lg font-semibold transition-colors duration-200"
            >
              Start Free Trial
              <ArrowRight className="w-5 h-5 ml-2 inline" />
            </button>
            <button className="border border-blue-300 text-white hover:bg-blue-500/20 px-8 py-4 rounded-lg font-semibold transition-colors duration-200">
              Schedule Demo
            </button>
          </div>
          
          <p className="text-blue-200 text-sm mt-6">
            No credit card required • 14-day free trial • Cancel anytime
          </p>
        </div>
      </section>
    </div>
  )
}

export default NexusLandingPage
